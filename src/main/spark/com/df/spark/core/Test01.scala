package com.df.spark.core

object Test01 {
    def main(args: Array[String]): Unit = {

        //val seq = for(i <- 1 to 10 ) yield i%3

        //println(seq)

        for (i <- 1 to 10) {
            println(i)
        }

    }
}
